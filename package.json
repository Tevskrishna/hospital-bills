{
  "name": "hospital-bills",
  "version": "25.0.0-lts",
  "description": "Mallareddy Hospital family bill tracker",
  "private": true,
  "scripts": {
    "start": "python -m http.server 8080",
    "test": "node --test tests/*.test.js",
    "test:coverage": "node --experimental-test-coverage --test-coverage-include=js/** --test tests/*.test.js",
    "build:production": "node scripts/build-production.js"
  },
  "devDependencies": {
    "javascript-obfuscator": "^4.1.1",
    "terser": "^5.39.0"
  }
}
